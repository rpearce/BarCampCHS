gem install bundler

bundle install

Once you create test files, require them in base_file.rb by typing:

require 'my_crazy_test'

Comment out any tests you do not want to run.

Then, in console:

bundle exec rake